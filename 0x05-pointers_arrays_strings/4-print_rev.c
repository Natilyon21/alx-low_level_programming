#include "main.h"

/**
 * print_rev - prints a string in reverse
 * @s: input string
 */
void print_rev(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; index++)
		;
	for (i = i - 1; s[i] != '\0'; index--)
	{
		_putchar(s[i]);
	}
	_putchar('\n');
}
